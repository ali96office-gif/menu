# تعليمات تشغيل المنيو الرقمي

## 1. إنشاء مشروع Firebase
1. روح على https://console.firebase.google.com
2. أنشئ مشروع جديد (مثلاً: digital-menu)
3. من القائمة الجانبية:
   - **Authentication → Sign-in method** → فعّل "Email/Password" واضغط Save
   - **Authentication → Users** → اضغط Add user وأضف إيميلك وكلمة سر (هذا حساب دخولك للوحة التحكم)
   - **Realtime Database** → Create Database → اختر أي منطقة → ابدأ بوضع test mode (راح نغيرها بعدين بالقواعد)

> ملاحظة: تجاوزنا خدمة Storage عمداً لأنها الحين تحتاج ترقية الخطة لـ Blaze (تتطلب بطاقة ائتمانية). صور الأصناف بهذا النظام تنحط برابط جاهز بدلاً من رفع مباشر — شرح الطريقة بالأسفل.

## 2. جلب إعدادات المشروع
من Project settings (⚙️) → Your apps → أضف تطبيق ويب (</>) → انسخ القيم اللي تطلع لك.

افتح ملف `firebase-config.js` وعوّض كل قيمة PASTE_YOUR_... بالقيمة الصحيحة من مشروعك، وتأكد من `databaseURL` بالضبط من صفحة Realtime Database.

## 3. رفع قواعد الحماية (مهم جداً)
- **Realtime Database → Rules**: الصق محتوى ملف `database.rules.json` واضغط Publish

## 4. رفع الملفات على GitHub
ارفع هذي الملفات لمستودعك (نفس طريقتك المعتادة عبر واجهة GitHub):
- `index.html` (صفحة المنيو للزبون)
- `admin.html` (لوحة التحكم)
- `firebase-config.js`

(ملف `database.rules.json` مو لازم يترفع للموقع، هذا فقط يُلصق بالكونسول كما بالخطوة 3)

## 5. النشر على Vercel
اربط نفس المستودع بـ Vercel زي مشاريعك السابقة، وراح يطلع لك رابط الموقع.

## 6. الدخول للوحة التحكم
افتح: `https://your-domain.vercel.app/admin.html`
سجل دخول بالإيميل وكلمة السر اللي سويتهم بخطوة 1.

من هناك تكدر:
- تضيف تصنيفات (تبويب "التصنيفات")
- تضيف أصناف بالاسم والصورة والحجم والسعر والنكهات والعروض (تبويب "الأصناف")
- تغير اسم المحل واللوگو والألوان (تبويب "الشكل العام")
- تشوف رابط المنيو والباركود القابل للتحميل (تبويب "رابط وباركود المنيو")

## 7. رابط الزبون
الزبون يفتح: `https://your-domain.vercel.app/index.html`
اطبع الباركود مال هذا الرابط وحطه على الطاولات.

---
### كيفية إضافة صورة لأي صنف (بدون رفع مباشر)
1. صوّر الصنف بجوالك
2. افتح موقع مجاني مثل **postimages.org** أو **imgbb.com** (ما يحتاج تسجيل حساب)
3. ارفع الصورة هناك، وانسخ "الرابط المباشر" (Direct link)
4. الصق هذا الرابط بحقل "رابط صورة الصنف" بلوحة التحكم واحفظ

### ملاحظات
- كل تعديل بلوحة التحكم ينعكس فوراً بمنيو الزبون بدون ما يحتاج يسوي تحديث للصفحة.
- اللوگو مال المحل بالإعدادات العامة نفس الشي — رابط صورة فقط.
